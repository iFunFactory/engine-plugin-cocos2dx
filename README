It is a plugin for Cocos2D-x to integrate game clients with iFunFactory Funapi.
You can copy files below into your project.

src/funapi_network.h
src/funapi_network.cc
rapidjson/  (Note: directory itself.)

To see how to use the plugin, please refer to src/funapi_network_tester.cc.
You can build the tester program like this:

$ mkdir build
$ cd build
$ cmake ..
$ make
$ ./src/funapi_plugin_cocos2dx-tester
